#include "tree.h"

/*
typedef struct s_tree
{
	temphabet		type;
	void			*content;
	int				priority;
	struct s_tree	*left_tree;
	struct s_tree	*right_tree;
}					t_tree;
*/

int	string_to_token(char **string, t_token **tokens)
{
	if ()
	return (0);
}
